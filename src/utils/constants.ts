export const API_URL = 'https://localhost:44386/api/';

export const TASK = {
  GET_TASKS: `${API_URL}task`,
  DELETE: `${API_URL}task`,
  POST: `${API_URL}task`,
  PUT: `${API_URL}task`
}

export const AUTH = {
  POST: `${API_URL}auth`,
}
